# Calque orchestrator + worker environment
# Copy to /opt/calque/.env on the host (do NOT commit the real file).

# Orchestrator
PORT=3002
WORKER_URL=http://127.0.0.1:8001
STATIC_DIR=/opt/calque/app/static-site
RUST_LOG=info

# Worker (uvicorn binds 127.0.0.1:8001 — see calque-worker.service)
